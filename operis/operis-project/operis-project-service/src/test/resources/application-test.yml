

spring:
    # Override DataSource Configuration pour les tests en utilisant H2 en mémoire
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;DATABASE_TO_UPPER=false;MODE=MYSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
    # Override Hibernate Configuration jpa pour les tests
  jpa:
    hibernate:
      ddl-auto: create-drop
      show-sql: true
#    h2:
#      console:
#        enabled: true